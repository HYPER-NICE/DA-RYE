<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hyper.darye.dao.CategoryDao">

    <!-- 모든 카테고리 조회 (삭제되지 않은 데이터만) -->
    <select id="selectAll" resultType="hyper.darye.model.Category">
        SELECT * FROM category WHERE deleted_date IS NULL
    </select>

    <!-- ID로 특정 카테고리 조회 (삭제되지 않은 데이터만) -->
    <select id="selectCategoryById" parameterType="long" resultType="hyper.darye.model.Category">
        SELECT * FROM category WHERE id = #{id} AND deleted_date IS NULL
    </select>

    <!-- 이름으로 특정 카테고리 조회 (삭제되지 않은 데이터만) -->
    <select id="selectCategoryByName" parameterType="string" resultType="hyper.darye.model.Category">
        SELECT * FROM category WHERE name = #{name} AND deleted_date IS NULL
    </select>

    <!-- 특정 카테고리의 직계 하위 카테고리 조회 (삭제되지 않은 데이터만) -->
    <select id="selectDirectSubCategoriesById" parameterType="long" resultType="hyper.darye.model.Category">
        SELECT * FROM category WHERE parent_id = #{parentId} AND deleted_date IS NULL
    </select>

    <!-- 특정 카테고리를 부모로 가지는 전체 하위 카테고리 조회 (삭제되지 않은 데이터만) -->
    <select id="selectAllSubCategories" parameterType="long" resultType="hyper.darye.model.Category">
        WITH RECURSIVE subcategories AS (
            SELECT * FROM category WHERE id = #{parentId} AND deleted_date IS NULL
            UNION ALL
            SELECT c.* FROM category c
                                INNER JOIN subcategories sc ON c.parent_id = sc.id
            WHERE c.deleted_date IS NULL
        )
        SELECT * FROM subcategories WHERE id != #{parentId};
    </select>

    <!-- 이름으로 특정 카테고리를 부모로 가지는 직계 하위 카테고리 조회 -->
    <select id="selectDirectSubCategoriesByName" parameterType="string" resultType="hyper.darye.model.Category">
        SELECT c.id, c.name, c.parent_id AS parentId
        FROM category c
                 INNER JOIN category p ON c.parent_id = p.id
        WHERE p.name = #{parentName}
          AND c.deleted_date IS NULL
          AND p.deleted_date IS NULL
    </select>

    <!-- 이름으로 특정 카테고리를 부모로 가지는 전체 하위 카테고리 조회 -->
    <select id="selectAllSubCategoriesByName" parameterType="string" resultType="hyper.darye.model.Category">
        WITH RECURSIVE subcategories AS (
            SELECT c.id, c.name, c.parent_id AS parentId
            FROM category c
                     INNER JOIN category p ON c.parent_id = p.id
            WHERE p.name = #{parentName} AND c.deleted_date IS NULL AND p.deleted_date IS NULL
            UNION ALL
            SELECT child.id, child.name, child.parent_id AS parentId
            FROM category child
                     INNER JOIN subcategories parent ON child.parent_id = parent.id
            WHERE child.deleted_date IS NULL
        )
        SELECT * FROM subcategories;
    </select>

    <!-- 카테고리 추가 -->
    <insert id="insertCategory" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO category (name, parent_id)
        VALUES (#{name}, #{parentId})
    </insert>

    <!-- 소프트 삭제 (deleted_date 설정) -->
    <update id="deleteCategory">
        UPDATE category
        SET deleted_date = CURRENT_TIMESTAMP(6)
        WHERE id = #{id} AND deleted_date IS NULL
    </update>

    <!-- 카테고리 업데이트 -->
    <update id="updateCategory">
        UPDATE category
        SET name = #{name}, parent_id = #{parentId}, last_modified_date = CURRENT_TIMESTAMP(6)
        WHERE id = #{id} AND deleted_date IS NULL
    </update>

</mapper>
